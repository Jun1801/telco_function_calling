# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment and secrets
.env
.env.*
!.env.example
*.key
*.pem
secrets.*

# Jupyter / Colab
.ipynb_checkpoints/
*.ipynb_checkpoints/

# Build / packaging
build/
dist/
*.egg-info/

# Logs
*.log
logs/

# Model and training artifacts
checkpoints/
models/
outputs/
runs/
wandb/
mlruns/
*.safetensors
*.bin
*.pt
*.pth
*.ckpt
*.gguf

# Large generated datasets / reports
data/public_warmup_subset.jsonl
# Public warm-up data (normalized HF datasets) + derived warm-up SFT — large, regen via prepare_public_warmup.py
data/public_warmup*.jsonl
data/sft_train_with_warmup*.jsonl
data/sft_train_real_with_warmup.jsonl
data/sft_eval_real_messages.jsonl
# Local staging copy of data files to upload to Drive (Colab)
drive_upload/
# Derived / pipeline artifacts (regenerated by training & rollout steps)
data/mlx_sft*/
data/rollouts.jsonl
data/corrections.jsonl
data/sdpo_rollouts.jsonl
data/sdpo_dataset.jsonl
# Kaggle generation artifacts (outputs-2 = canonical reference, regen on Kaggle)
data/real_data/
#data/sft_train.jsonl
#data/sft_eval.jsonl
reports/*.jsonl
reports/*.csv

# Keep report notes if written manually
!reports/*.md

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
